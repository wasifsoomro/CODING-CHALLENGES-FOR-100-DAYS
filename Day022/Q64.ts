//64
//Create a function with two parameter
function combineStringAndNumber(text: string, age: number): string{
    return text + age //Combine string and number
  }
  console.log(combineStringAndNumber("Age:", 30)) //pass two arguments that are required