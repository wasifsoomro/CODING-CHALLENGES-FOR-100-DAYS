// Create a Imediately invoked function execution(IIFE) 

(function userProfile(){
    let name: string = "Wasif" //create a vraible and store a name
    let age: number = 19   // craete a variable and store age
    console.log(`Name ${name} \n Age ${age}`) //print a name and age
})() //Call self directed no need to invoke manually