let idCard: {
    name: string,
    surname: string,
    rollNo: number,
} = {
    name: "Wasif",
    surname: "Soomro",
    rollNo: 365141
}
console.log(`Name ${idCard.name} surname ${idCard.surname} and rollNo  is ${idCard.rollNo}`)