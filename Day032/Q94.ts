//94
//create a array
let arr: string[] = ["Wasif", "Soomro"]
//create a new array with the length of each element present in array
let length_arr: number[] = arr.map(len => len.length)
console.log(length_arr)//print a length of element