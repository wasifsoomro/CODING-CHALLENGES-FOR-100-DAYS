//87
//create a function pass one parameter 
function stringValue(str) {
    return str.substring(6, 18); //it return a substring start from character available on index 6 and end with 18 -1 mean on index 17
}
console.log(stringValue("learn to code with javascript")); //print a result
export {};
